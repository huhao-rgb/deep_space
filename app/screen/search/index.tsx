import type { FC } from 'react'

import type { RouteProps } from '@/types'

const Search: FC<RouteProps<'Search'>> = (props) => {
  console.log(props.navigation)

  return (
    <></>
  )
}

export default Search
